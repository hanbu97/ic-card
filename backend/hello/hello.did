type Shop = record {
  name : text;
};

service : {
  greet : (text) -> (text) query;
  whoami : () -> (text) query;
  create_shop : (text) -> ();
  get_shops : () -> (vec Shop) query;
  all_shops : () -> (vec text) query;
};
